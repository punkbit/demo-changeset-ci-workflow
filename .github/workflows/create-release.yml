name: 📦 Create Release

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release type'
        required: true
        type: choice
        options:
          - latest
          - rc
          - test
        default: 'rc'

jobs:
  create-release-pr:
    name: Create Release PR
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm install

      - name: Check pre-release status
        id: check-prerelease
        run: |
          CURRENT_TAG="N/A"

          if [[ -f .changeset/pre.json ]]; then
            CURRENT_TAG=$(node -p "require('./.changeset/pre.json').tag")
            echo "prerelease=true" >> $GITHUB_OUTPUT
            echo "current_tag=$CURRENT_TAG" >> $GITHUB_OUTPUT
          else
            echo "prerelease=false" >> $GITHUB_OUTPUT
            CURRENT_TAG="${{inputs.release_type}}"
          fi

          echo "💡 Current tag is $CURRENT_TAG"

      - name: Validate latest release must be from main
        if: inputs.release_type == 'latest' && github.ref_name != 'main'
        run: |
          echo "👹 Oops! Latest releases must be created from the 'main' branch."
          echo "💡 Current branch is '${{ github.ref_name }}'. Please switch to 'main' and try again."
          exit 1

      - name: Switch prerelease mode
        if: steps.check-prerelease.outputs.prerelease == 'true' && steps.check-prerelease.outputs.current_tag != inputs.release_type && inputs.release_type != 'latest'
        run: |
          echo "🔄 Switching from ${{ steps.check-prerelease.outputs.current_tag }} to ${{ inputs.release_type }} prerelease mode"
          if ! npx changeset pre exit; then
            echo "👹 Oops! Failed to exit current prerelease mode for some reason..."
            exit 1
          else
            echo "✅ Exited ${{ steps.check-prerelease.outputs.current_tag }} prerelease mode"
          fi

      - name: Enter prerelease mode
        if: inputs.release_type != 'latest' && (steps.check-prerelease.outputs.prerelease != 'true' || steps.check-prerelease.outputs.current_tag != inputs.release_type)
        run: |
          echo "🤖 Entering ${{ inputs.release_type }} prerelease mode"
          npx changeset pre enter ${{ inputs.release_type }}

      - name: Validate latest release eligibility
        if: inputs.release_type == 'latest' && steps.check-prerelease.outputs.prerelease != 'true'
        run: |
          echo "👹 Oops! Cannot release 'latest' without a pre-release version first..."
          echo "💡 You must release through 'test' or 'rc' before creating a stable release."
          exit 1

      - name: Validate maintenance branch exists for latest release
        if: inputs.release_type == 'latest'
        run: |
          echo "🔍 Checking if maintenance branch exists for latest release..."
          
          # Get the version that will be released (before versioning)
          # We need to check what version changeset will compute
          # For now, we'll check in the publisher - here we just verify we're on main
          echo "✅ On main branch, maintenance branch check will happen in publisher"

      - name: Confirm production release
        if: inputs.release_type == 'latest'
        run: |
          echo "⚠️WARNING: Creating a STABLE production release!"
          echo "This will publish to npm with the 'latest' tag."
          echo "Make sure you've reviewed all changes carefully, please!"

      - name: Version package
        id: version-package
        run: |
          echo "📦 Versioning packages..."
          if ! npm run changeset:version; then
            echo "👹 Oops! Failed to do changeset versioning for some reason..."
            exit 1
          else
            echo "✅ Versioned package successfully!"
          fi

          VERSION=$(node -p "require('./package.json').version")
          PKG_NAME=$(node -p "require('./package.json').name")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "package_name=$PKG_NAME" >> $GITHUB_OUTPUT

      - name: Determine PR details
        id: pr-details
        run: |
          echo "title=chore: 🤖 release v${{ steps.version-package.outputs.version }} (${{ inputs.release_type }})" >> $GITHUB_OUTPUT
          echo "branch=changeset-release/v${{ steps.version-package.outputs.version }}-${{ inputs.release_type }}" >> $GITHUB_OUTPUT

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: ${{ steps.pr-details.outputs.title }}
          base: ${{ github.ref_name }}
          branch: ${{ steps.pr-details.outputs.branch }}
          delete-branch: true
          title: ${{ steps.pr-details.outputs.title }}
          body: |
            ## Why?

            To trigger a release of ${{ steps.version-package.outputs.package_name }} version ${{ steps.version-package.outputs.version }} (${{ inputs.release_type }}).

            ## Release checklist?

            Take this opportunity to ensure that the following checklist is verified to avoid disappointment.

            - [ ] Version bump is correct
            - [ ] Changelog entries are accurate
            - [ ] All changesets have been consumed
            - [ ] Ready to publish

            ## Preview?

            Optionally, provide a demo or a preview url here

            ---

            **Tag:** `v${{ steps.version-package.outputs.version }}` will be included in this PR
