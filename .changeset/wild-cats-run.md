---
"@punkbit/demo-changeset-ci-workflow": major
---

BREAKING: Refactor API response structure for better consistency

The response object now wraps all data in a `data` field and errors in an `errors` field.
